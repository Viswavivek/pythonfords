## Use Stackoverflow survey data or your use choice of data

1.Load a sample DataFrame from a CSV file.
2.Set one of the columns as the index using set_index().
3.Display the DataFrame to verify the index has been set.
4.Use the DataFrame from the previous exercise.
5.Reset the index using reset_index().
6.Display the DataFrame to confirm the index has been reset.
7.Create a DataFrame with a hierarchical index using set_index with multiple columns.
8. Demonstrate selecting data using the hierarchical index.
9.Load a DataFrame with a datetime column.
10.Set the datetime column as the index.
11.Filter the DataFrame to include only rows within a specific date range.
12.Load a DataFrame with numerical and categorical columns.
13.Use loc to filter rows based on specific conditions for both rows and columns.
